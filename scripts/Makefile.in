# Here also go all build targets specific to this very newgrf
$(CHANGELOG_FILE):
	$(_E) [Generating changelog]
	$(_V) $(shell) make_changelog.sh > docs/changelog.txt
	
$(SRC_DIR)/$(FILENAME_STUB).pnml: $(SRC_TOWN)
	$(_E) "Building $@ from $<"
	
$(NUMBER_FILE):
	$(_E) "[Count City]"
	$(_V) $(shell) count_town.sh $@

	
$(SRC_TOWN):
	$(_E) "Building $@"
	$(_V) $(shell) generate_town.sh $@
